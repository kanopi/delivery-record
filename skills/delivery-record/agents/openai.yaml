policy:
  allow_implicit_invocation: false
interface:
  display_name: "CMS Cultivator: Delivery Record"
  short_description: "Draft a schema-typed Delivery Record for an AI-assisted output; refuses to write without a named human reviewer and checkpoint notes, then indexes it in Teamwork."
